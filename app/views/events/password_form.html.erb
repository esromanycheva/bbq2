<h1><%= t('.access') %></h1>

<%= form_tag('', method: 'post') do |f| %>
  <div class="form-group">
    <%= label t('.pin_code'), :pincode %>
    <%= text_field_tag :pincode, params[:pincode], class: 'form-control' %>
  </div>

  <div class="actions">
    <%= submit_tag %>
  </div>
<% end %>

<%= link_to t('.root'), events_path %>
